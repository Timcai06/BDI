# PROGRESS

## 作用

这份文档只写当前已确认的事实和当前重点。

## 当前状态

- 已完成项目方向判断
- 已完成系统结构思路
- 已完成项目核心目标文档
- 已建立 `AGENTS` 文档体系
- 已建立 `backend/` 与 `frontend/` 骨架
- 已完成 `GET /health`、`POST /predict` 与结果管理闭环
- 已完成真实 `YOLOv8-seg` 单图联调验收
- 已完成结果展示、导出、历史回看
- 已完成多模型骨架第一轮建设
- 已实现 `inference_mode` 和 `is_available`

## 当前重点

- 接入第二个真实模型版本
- 验证“同类模型换权重即可切换”
- 增强 `mask` 可视化表达
- 保持前后端统一协议稳定

## 阶段结论

- `Phase 2` 已完成
- `Phase 3` 已完成
- `Phase 4` 已完成第一轮骨架

## 下一步

- 继续同步入口、计划与记忆文档
- 完成第二个真实模型版本联调
- 评估是否补充更强的 `mask` 展示
- 继续推进 `sliced` 和 batch 方向

import { HistoryRouteShell } from "@/components/history-route-shell";

export const metadata = {
  title: "History - BDI Infrastructure Scan",
};

export default function DashboardHistoryPage() {
  return <HistoryRouteShell />;
}

# 项目核心目标

## 作用

这份文档只回答一件事：这个项目最终要做成什么。

## 目标

- 以 `YOLOv8` 为当前核心推理引擎
- 兼容 `YOLOv8` 优化变体和后续模型版本
- 保持前后端协议稳定
- 输出病害类别、`Mask`、置信度和可扩展量化字段
- 同时满足“核心算法系统”和“实际应用展示”

## 约束

- 不把系统做成绑定单一权重的 demo
- 不允许模型升级频繁破坏接口
- 不允许只强调算法指标而忽略展示闭环

## 结论

**这是一个以 `YOLOv8` 为起点、可持续演进的桥梁病害识别系统原型。**

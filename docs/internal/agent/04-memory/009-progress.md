# PROGRESS

## 作用

这份文档只写当前已确认的事实和当前重点。

## 当前状态

- 已完成项目方向判断
- 已完成系统结构思路
- 已完成项目核心目标文档
- 已建立 `AGENTS` 文档体系
- 已建立 `backend/` 与 `frontend/` 骨架
- 已完成 `GET /health`、`POST /predict` 与结果管理闭环
- 已完成真实 `YOLOv8-seg` 单图联调验收
- 已完成结果展示、导出、历史回看
- 已完成多模型骨架第一轮建设
- 已实现 `inference_mode` 和 `is_available`
- 已完成双模型融合推理（通用检测 + 渗水专项），支持来源标注与实例级对比
- 已完成 `Phase 5` 方向重定义：从单图 demo 转向真实巡检业务流程系统化
- 已完成数据库基础接入：`SQLAlchemy`、`Alembic`、`PostgreSQL` 驱动
- 已完成批次主链路核心数据模型与迁移脚本
- 已完成 `v1` 批次工作流基础接口与本地异步 worker
- 已完成 `detections / reviews / alerts` API 第一版
- 已完成识别后规则型自动预警触发（按病害数量阈值与重点类别命中）
- 已完成病害检索、复核记录、预警列表的排序与组合筛选参数
- 已完成企业化前端第一版：巡检工作台、图片详情复核页、告警中心页
- 已完成巡检工作台 URL 查询参数持久化与批次分页
- 已完成巡检工作台"创建批次 + 批量图片上传 + 自动入队"入口联调
- 已完成图片详情页复核历史时间线
- 已完成告警中心批量确认与批量关闭
- 已完成 Phase 5 后端单元测试与路由契约测试，`PostgreSQL` 集成测试已落位（未配置集成库时自动跳过）
- 已完成 ops 工作台从展示型到处置型的改造
- 已完成桥梁资产作为一级入口的 UX 重构
- 已完成批次控制精简与卡片式条目选择
- 已完成共享页面壳统一，消除各页面头部不一致
- 已完成历史流程精简与 lab 死状态清理
- 已完成 `Phase 6` 目标定义与计划落位（稳定性、决策能力、运营效率、集成能力）
- 已完成侧边栏"桥梁资产"导航入口（位于运营总览与批次工作台之间）
- 已完成 `/dashboard/bridges` 桥梁资产列表页与 `/dashboard/bridges/[bridgeId]` 资产驾驶舱详情页
- 已完成 Ops 工作台 Bento 式分步导航布局（选桥 → 批次工作台，动画箭头连接）
- 已完成批次摘要栏可折叠 redesign（图标化指标卡片、动画进度条）
- 已完成巡检档案中心桥梁资产筛选器与单图库切换按钮
- 已完成全量页面中文化（英文标签 → 中文，按钮图标化，圆角统一升级）
- 已全面接入 Framer Motion 动画（页面入场、状态提示、进度条、折叠面板）
- 已实现预测客户端 demo 降级（无后端时自动回退到演示产物路径）
- 已完成全量文案精简：去除冗余修饰词，统一为简洁直白表述
- 已完成素材列表（Item Grid）重设计：状态 badge 中文化、选择操作栏条件显示 + AnimatePresence 折叠、表头全中文
- 已完成工作台骨架屏（OpsWorkbenchSkeleton）：AnimatePresence mode="wait" 实现三态平滑切换
- 已实现 notice 提示 3 秒自动消失
- 已优化滚动性能：移除部分面板 backdrop-blur
- 已完成增强处理可选化：enhancement_mode 默认值从 auto 改为 always
- 已完成事后增强接口 POST /results/{image_id}/enhance
- 已完成增强按钮交互变更：从切换已有结果变为按需生成
- 已完成掩膜可视化优化：多边形检测框增加填充色与透明度

## 当前重点

- 建立任务稳定性指标基线（成功率、重试恢复率、P50/P95）
- 完成规则引擎首版定义（评级、优先级、建议时限）
- 补齐 critical finding 与告警时效追踪字段
- 推进 dashboard 从“展示型”到“处置型”优化
- 推进最小工单接口与导出规范

## 阶段结论

- `Phase 2` 已完成
- `Phase 3` 已完成
- `Phase 4` 已全部完成
- `Phase 5` 后端核心链路 + 前端 ops 工作台已基本收口
- `Phase 6` 已进入优化实施阶段

## 下一步

- 完成稳定性指标埋点与看板基础
- 完成规则引擎首版接口与字段对齐
- 完成 critical finding 模板落地与联调
- 完成高风险优先处置视图与批量处置链路
